#pragma once

#include <string>

namespace ii::err {
	const std::string runtime1 = "We could not export from module, because it was not loaded. Module: %s";
	const std::string runtime2 = "Dos header is wrong of module ";
	const std::string runtime3 = "Could not found nt header for ";
	const std::string runtime4 = "Export directory could not read for ";
	const std::string runtime5 = "Export directory was empty for ";
	const std::string runtime6 = " not found in module ";
}
